
from configparser import Interpolation
from tkinter import Frame
import cv2
from cv2 import IMREAD_UNCHANGED
from cv2 import imread
from cv2 import IMREAD_GRAYSCALE
from cv2 import imshow 
from cv2 import imwrite 
from cv2 import waitKey
from cv2 import destroyAllWindows
from cv2 import resize

print(cv2.__version__)

img = imread('Image/pup.jpg')
imshow('Puppy', img) #show the image

# #reading video

# capture = cv2.ImageCapture('Videos/pup.jpg')

# while True:
#     isTrue, frame = capture.read()

#     frame.resized = rescaleFrame(frame, scale = .2)

#     imshow('Video', frame)
#     imshow('Video Resized', frame.resized)

#     if waitKey(20) & 0xFF==ord('d'):
#         break
